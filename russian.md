다음은 요청하신 **러시아어 번역**입니다.

---

**Итоговый экзамен (углублённый курс языка C)**
**Кафедра компьютерной инженерии, Национальный университет Кёнггук**

1. В среде Visual Studio CMD напишите только команду для создания `my.lib` из двух функций `add.o`, `sub.o`.

2. В WSL имеется makefile-проект, в котором функции `add.c`, `sub.c` вызываются из `main.c`. Какая команда используется для запуска?

3. При вводе
   `c:\temp\>math 10 + 20`
   в функции
   `int main(int argc, char* argv[])`
   чтобы вывести на экран символ `+`, что нужно подставить в
   `printf("%c\n", __________);`?

4. При сортировке целочисленного массива с помощью функции `qsort` какое общее выражение должно быть вставлено в оба места в скобках?

   ```c
   int cmp(const void* a, const void* b) {
       return (*(_____)a - *(_____)b);
   }
   ```

5. Заполните пропуски для определения структуры узла бинарного дерева
   (узел должен ссылаться сам на себя).

   ```c
   struct Node {
       int data;
       _______ left;
       _______ right;
   };
   ```

6. Нарисуйте блок-схему программы, которая складывает числа от 1 до 10
   (используйте правильные обозначения блок-схем).

7. Каково конечное возвращаемое значение при вызове следующего кода?
   (укажите одно значение)

   ```c
   int rval1(){ return 2; }
   int rvla2(){ return 4 + rval1(); }
   ```

8. Напишите функцию `swap()`, которая меняет местами два значения массива `{11, 33}`.

   ```c
   void swap(int *a, int *b) {
       int t;
       // напишите код
   }
   ```

9. Какая из следующих идей наиболее точно отражает суть быстрой сортировки (Quick Sort)?
   ① Всегда перемещать среднее значение в начало
   ② Повторно отправлять наибольшее значение в конец
   ③ Разделение по опорному элементу и рекурсивная сортировка
   ④ Сортировка на основе структуры кучи

10. Какое описание наиболее правильно характеризует псевдокод и блок-схемы?
    ① Должны строго следовать синтаксису C
    ② Это рисунки для представления структур данных
    ③ Текстовое/графическое пошаговое описание алгоритма
    ④ Компилируемый код

11. Какое утверждение наиболее точно описывает различие между стеком и очередью?
    ① Стек FIFO, очередь LIFO
    ② Стек LIFO, очередь FIFO
    ③ Оба FIFO
    ④ Оба основаны на деревьях

12. Какое утверждение о быстрой сортировке является наиболее верным?
    ① Всегда гарантирует O(n log n)
    ② Это алгоритм «разделяй и властвуй»
    ③ Всегда быстра для связных списков
    ④ Формирует полное бинарное дерево

13. Зачем в Visual Studio устанавливается `/SUBSYSTEM:WINDOWS`?
    ① Для использования аргументов `main`
    ② Для запуска оконного приложения без консоли
    ③ Для использования CPU-рендеринга
    ④ Для автоматического создания библиотек

14. Какое утверждение о указателях верно?
    ① Переменная для хранения значений
    ② Переменная для хранения возвращаемого значения
    ③ Переменная для хранения адреса памяти
    ④ Инструмент расширения размера массива

15. ( O / X ) Симметричный обход (Inorder Traversal):
    левый узел → сам узел → правый узел.

16. ( O / X ) Массивы в языке C хранятся в непрерывной области памяти.

17. В следующем коде указатель `p` хранит (    ) переменной `a`.

    ```c
    int a = 10;
    int *p = &a;
    ```

18. В функции быстрой сортировки укажите, что общее должно быть вставлено в `_____`.

    ```c
    #include <stdio.h>
    void q(int *a,int l,int r){
        if(l>=r) return;
        int p=a[r],i=l-1,t;
        for(int j=l;j<r;j++) if(a[j]<=p)
            { t=a[++i]; a[i]=a[j]; a[j]=t; }
        t=a[i+1]; a[i+1]=a[r]; a[r]=t; 
        _____(a,l,i); _____(a,i+2,r);
    }
    ```

19. Используя raylib на языке C, напишите команды для частей `____`, чтобы открыть окно размером 800×600 и нарисовать круг радиуса 50 в центре экрана.

    ```c
    #include <raylib.h>
    int main(void) {
        _____(800, 600, "ANU");
        while (!WindowShouldClose()) {
            _____();  
            DrawCircle(400, 300, 50, RED);
            _____();
        }
        CloseWindow();
        return 0;
    }
    ```

20. При выполнении следующего C-кода какова разница (в байтах) между адресами памяти, на которые указывают `p` и `q`?
    (предполагается, что `sizeof(int) = 4` байта)

    ```c
    int arr[10]; 
    int *p = arr; 
    int *q = arr + 3;
    ```

---

**[Письменное программирование]**
Для каждого пункта выберите и выполните **только одно задание**.

21. [10] Напишите программу, которая инициализирует два числа в памяти, передаёт их в функцию как параметры, складывает, возвращает результат и выводит его на экран.

22. [10] Из структур данных выберите **только один** вариант и реализуйте его:

1) Стек
2) Список
3) Бинарное дерево

23. [10] Из алгоритмов выберите **только один** вариант и реализуйте его:

1) Пузырьковая сортировка
2) Генерация случайных чисел на основе медианы
3) Вычисление 5! с использованием рекурсии
4) Вывод только чисел, кратных 3, из диапазона от 1 до 30

24. [10] Из симуляций выберите **только один** вариант и реализуйте его:

1) Подбрасывание 1000 монет и подсчёт орлов и решек
2) Подбрасывание кубика 6000 раз и подсчёт частоты каждого числа

25. Комментарий или слова благодарности по поводу данного курса

---

필요하면 **전문 용어를 더 엄밀한 CS 러시아어 표현**으로 다듬어 줄 수도 있어요.

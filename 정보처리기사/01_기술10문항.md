좋아요! 아래는 정보처리기사(실기 중심, 일부 필기형 포함)에서 자주 나오는 **C 언어 기출 유형 10문항**을 간단히 정리한 것입니다. 실제 기출을 바탕으로 **형식과 난이도**를 맞춰 **요구사항만 간결히** 제시했어요(각 문항 끝의 출처 참고).

1. 다음 코드의 **출력 결과**를 쓰시오.

```c
int a[5] = {2, 1, 5, 3, 4};
for (int i=0;i<4;i++)
  for (int j=0;j<4-i;j++)
    if (a[j] > a[j+1]) { int t=a[j]; a[j]=a[j+1]; a[j+1]=t; }
for (int i=0;i<5;i++) printf("%d ", a[i]);
```

(정렬 원리 파악) ([Velog][1])

2. 다음 코드의 **출력 결과**를 쓰시오.

```c
int i=0, c=1;
while (i<5) { i++; c *= i; }
printf("%d", c);
```

(반복문 변수 갱신 순서) ([Velog][1])

3. 다음의 **함수 호출 결과**를 쓰시오.

```c
int r1(){ return 4; }
int r10(){ return 30 + r1(); }
int r100(){ return 200 + r10(); }
int main(){ printf("%d\n", r100()); }
```

(함수 중첩 호출) ([Velog][1])

4. **구조체 멤버 접근**에 사용되는 연산자를 쓰시오.

* 포인터가 가리키는 구조체의 멤버 접근 연산자: (    )
  (기호 자체를 답하시오) ([김테드 개발 블로그][2])

5. 다음 포인터 코드의 **출력 결과**를 쓰시오.

```c
int a[4]={0,2,4,8}, b[3]={0}, sum=0, *p;
for (int i=1;i<4;i++){
  p = a + i;
  b[i-1] = *p - a[i-1];
  sum += b[i-1] + a[i];
}
printf("%d", sum);
```

(포인터 산술·배열 인덱싱) ([잡코딩 정보 블로그][3])

6. 다음은 2차원 배열을 이용한 **지뢰(‘*’) 배치 결과**를 묻는 문제이다. 예시 출력 형식을 참고하여, 주어진 규칙에 따라 `mines` 배열의 **최종 문자 배열**을 작성하시오.
   (규칙 요약과 예시 형식 제공) ([잡코딩 정보 블로그][3])

7. 다음 코드의 **출력 결과**를 쓰시오.

```c
char s[]="abcde"; char *p=s;
p += 2; *p = 'Z';
printf("%s", s);
```

(문자열·포인터 오프셋) ([부자가 되고 싶은 셈이][4])

8. 다음 코드의 **출력 결과**를 쓰시오.

```c
int f(int n){ if(n<=1) return 1; return n * f(n-1); }
int main(){ printf("%d", f(5)/f(3)); }
```

(재귀·수학적 의미 파악) ([Life-Journey][5])

9. 다음 중 **비트 연산 결과**가 맞는 것을 고르시오.
   가) `12 & 10`  나) `12 | 10`  다) `12 ^ 10`
   ① 가=8, 나=14, 다=6  ② 가=10, 나=12, 다=6  ③ 가=8, 나=12, 다=4  ④ 가=10, 나=14, 다=6
   (정답만 고르는 객관식) ([발전하는 개발자][6])

10. 단일 연결리스트에서 **머리 삽입(push_front)** 다음 코드의 **빈칸**에 들어갈 식별자를 쓰시오.

```c
typedef struct Node{ int d; struct Node* next; } Node;
void push_front(Node** head, int v){
  Node* n = malloc(sizeof(Node));
  n->d = v; n->next = *head; *head = n;
}
int main(){
  Node* head = /* (    ) */;
  push_front(&head, 10);
  push_front(&head, 20);
  // ...
}
```

(포인터 이중 포인터 개념 점검) ([youtube.com][7])

—

### 참고/근거

최근 수년간의 정보처리기사 실기 C언어 복원·정리 자료를 토대로 유형을 선별·요약했습니다(정확한 원문 문구는 지양하고 형식만 맞춰 재구성). 대표 참고: 블로그/정리 포스트 및 회차별 예시들. ([부자가 되고 싶은 셈이][4])

원하시면 위 10문항의 **정답/해설**도 바로 이어서 제공해 드릴게요.

[1]: https://velog.io/%40mjieun/%EC%A0%95%EB%B3%B4%EC%B2%98%EB%A6%AC%EA%B8%B0%EC%82%AC-%ED%94%84%EB%A1%9C%EA%B7%B8%EB%9E%98%EB%B0%8D-%EC%96%B8%EC%96%B4-%EA%B8%B0%EC%B6%9C-%EB%AC%B8%EC%A0%9C-%EB%AA%A8%EC%9D%8C-C-%EC%96%B8%EC%96%B4?utm_source=chatgpt.com "[정보처리기사] 프로그래밍 언어 기출 문제 모음(2020년 1회"
[2]: https://idkim97.github.io/2024-03-28-%EC%A0%95%EB%B3%B4%EC%B2%98%EB%A6%AC%EA%B8%B0%EC%82%AC%20%EC%8B%A4%EA%B8%B0%20C%EC%96%B8%EC%96%B4%20%EB%AA%A8%EC%9D%8C/?utm_source=chatgpt.com "[정처기] 정보처리기사 실기 C언어 문제 모음 - 김테드 개발 블로그"
[3]: https://complainrevolutionist.tistory.com/38?utm_source=chatgpt.com "정보처리기사 실기 C언어 기출문제 모음"
[4]: https://roadtofree.tistory.com/entry/30?utm_source=chatgpt.com "정보처리기사 실기 C언어 기출문제 모음 (최근 5개년)"
[5]: https://chobopark.tistory.com/425?utm_source=chatgpt.com "정보처리기사 실기 C언어편 - Life-Journey - 티스토리"
[6]: https://yaongdaong.tistory.com/entry/%EC%A0%95%EC%B2%98%EA%B8%B0-C%EC%96%B8%EC%96%B4-%ED%95%84%EA%B8%B0-%EA%B8%B0%EC%B6%9C-%EB%AC%B8%EC%A0%9C-%ED%95%B4%EC%84%A4?utm_source=chatgpt.com "[정보처리기사] C언어 필기 기출 문제 해설 - 발전하는 개발자"
[7]: https://www.youtube.com/watch?v=_DujxMp8s04&utm_source=chatgpt.com "[정보처리기사 실기 2025년 2회]C언어 기출문제분석(싱글 ..."

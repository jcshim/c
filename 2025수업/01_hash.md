좋아요 😊
완전 초보 학생용으로, **진짜 간단하게 "해시(hash)" 개념**을 보여주는 C 예제를 아래처럼 만들 수 있습니다.
(“해시 테이블”이 아니라 **해시 함수가 뭔지**부터 감을 잡는 수준입니다.)

---

### 🧠 개념 먼저

> “해시 함수(hash function)”는 어떤 값을 **숫자로 바꾸는 함수**입니다.
> 예를 들어 이름 `"Tom"` → 숫자 2, `"Amy"` → 숫자 5 처럼 바꿔서
> 그 숫자를 **저장 칸(배열 인덱스)** 으로 쓰는 거예요.

---

### 💻 초간단 코드 예제

```c
#include <stdio.h>
#include <string.h>

#define SIZE 10   // 해시 테이블 크기 (10칸짜리)

int main(void) {
    char *names[] = {"Tom", "Amy", "Bob", "Jane"};
    int scores[]  = {90, 80, 70, 95};
    int table[SIZE];  // 10칸짜리 배열 (빈 해시 테이블)
    for (int i = 0; i < SIZE; i++) table[i] = -1; // -1은 "비어 있음" 표시

    // 간단한 해시 함수: 이름 첫 글자의 아스키코드 % SIZE
    for (int i = 0; i < 4; i++) {
        unsigned char first = names[i][0];
        int key = first % SIZE;    // 0~9 중 하나
        table[key] = scores[i];    // 해당 칸에 점수 저장
        printf("'%s' → key=%d → table[%d]=%d\n", names[i], key, key, scores[i]);
    }

    printf("\n=== 해시 테이블 내용 ===\n");
    for (int i = 0; i < SIZE; i++) {
        printf("[%d] : %d\n", i, table[i]);
    }

    return 0;
}
```

---

### 🧩 실행 결과 (예상)

```
'Tom' → key=4 → table[4]=90
'Amy' → key=5 → table[5]=80
'Bob' → key=8 → table[8]=70
'Jane' → key=4 → table[4]=95   ← 충돌(덮어씀)

=== 해시 테이블 내용 ===
[0] : -1
[1] : -1
[2] : -1
[3] : -1
[4] : 95
[5] : 80
[6] : -1
[7] : -1
[8] : 70
[9] : -1
```

---

### 🧩 핵심 설명

* `key = 첫 글자의 아스키코드 % 10` → 해시 함수
* `table[key] = 값` → 해시 테이블에 저장
* `"Tom"`과 `"Jane"`이 같은 key=4라서 덮어씀 → **충돌(collision)**
* 이런 충돌을 막기 위해 "체이닝"이나 "개방주소법" 등을 나중에 배웁니다.

---

원하시면, 이 코드에 “충돌을 막는 아주 쉬운 체이닝 버전(연결리스트)”도
같은 수준(초보자용 주석 포함)으로 만들어드릴까요?

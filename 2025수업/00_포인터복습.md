아래는 **대학교 1학년 대상**,
**1시간 이론 + 1시간 실습**으로 구성된
“포인터 총정리 특강” 커리큘럼입니다.

**목표:**

* 포인터 개념을 한 번에 정리하고,
* 배열/문자열/구조체/동적 메모리/함수 포인터까지 주요 활용을 체계적으로 복습
* 실습을 통해 실제 사용 능력 강화

---

# 📘 1시간 이론 강의안(60분)

## 0. 도입 (5분)

* 왜 C 언어에서 포인터가 중요한가?
  → **메모리 직접 접근**, **동적 메모리**, **자료구조**, **함수 인자 전달**, **시스템 프로그래밍**의 핵심
* 오늘 다룰 범위 소개:
  포인터 → 배열 → 문자열 → 구조체 → malloc/free → 이중포인터 → 함수 포인터 → 응용자료구조

---

## 1. 포인터 기본 개념 (10분)

### ● (1) 변수와 주소

* 변수는 메모리의 **공간**
* 주소는 **그 공간의 위치**

### ● (2) 포인터 선언

* `int *p;` → “int를 가리키는 포인터 p”
* `p = &a;` / `*p = 10;` 개념 설명
* **포인터의 3요소**
  주소 / 가리키는 값 / 가리키는 타입

### ● (3) 포인터 연산

* `p + 1` → 타입 크기만큼 이동
* 인덱싱: `p[i] == *(p+i)`

---

## 2. 포인터와 배열 (10분)

### ● 배열 이름과 포인터의 관계

* `int a[5];` → `a`는 첫 번째 원소 주소
* `int *p = a;`
* `a[i]`, `p[i]`

### ● 배열을 함수 인자로 전달할 때

* `void foo(int arr[])` → 사실 `int *arr`

### ● 2차원 배열과 포인터

* `int a[3][4]`
* `a[i]`, `a[i][j]`, `*(a+i)`

---

## 3. 문자열과 포인터 (10분)

### ● 문자열 리터럴과 char 포인터

* `char *s = "hello";` (읽기 전용)
* `char s[] = "hello";` (수정 가능)

### ● 문자열 함수들의 포인터 사용

* `strlen`, `strcpy`, `strcmp`

### ● 문자열 입력 시 포인터의 위험성

* `scanf("%s", s);`
* 버퍼 크기 관리 중요성

---

## 4. 동적 메모리 (10분)

### ● malloc / free

* `int *p = malloc(n * sizeof(int));`
* `free(p);`
* **메모리 누수, 댕글링 포인터** 개념

### ● 동적 2차원 배열 생성 패턴

* 패턴1: `int **a;`
* 패턴2: 1차원 블록으로 할당 후 인덱싱 `(i*m + j)`

---

## 5. 구조체와 포인터 (7분)

### ● 구조체 포인터

* `struct Node *p;`
* 접근: `p->data` / `(*p).data`
* 동적 구조체 생성
  `Node *n = malloc(sizeof(Node));`

### ● 구조체 배열 + 포인터

---

## 6. 이중 포인터(더블 포인터) (5분)

* 포인터를 가리키는 포인터
  `int **pp;`
* malloc에서 이중포인터가 필요한 이유
  (`int **ret` 패턴)
* 2차원 배열 관리에서의 사용

---

## 7. 함수 포인터(콜백) (8분)

### ● 함수 포인터 선언

`int (*f)(int, int);`

### ● 함수 포인터 배열

* 간단한 “사칙연산 계산기” 예시
  `ops[4] = {add, sub, mul, div};`
  `ops[i](a, b);`

### ● qsort 예제 소개

* 비교함수를 전달하는 대표적 사례

---

# 💻 1시간 실습 수업안(60분)

### 전체 실습 목표

* 학생들이 **포인터 문법** + **배열/문자열/구조체/동적 메모리/함수 포인터**를
  손으로 직접 실행하며 체득하도록 구성

---

## 실습 1: 포인터 맛보기 (10분)

**목표:** 주소·포인터·역참조 기본 이해

### 코드:

```c
int a = 10;
int *p = &a;

printf("a = %d\n", a);
printf("&a = %p\n", &a);
printf("p = %p\n", p);
printf("*p = %d\n", *p);

*p = 20;
printf("a(after) = %d\n", a);
```

학생에게 직접 주소 비교 시켜봄.

---

## 실습 2: 배열과 포인터 (10분)

### 코드:

```c
int a[5] = {1,2,3,4,5};
int *p = a;

for (int i = 0; i < 5; i++)
    printf("%d %d\n", a[i], p[i]);
```

* `p+1`, `*(p+2)` 시도
* 포인터 산술을 직접 확인

---

## 실습 3: 문자열과 포인터 (10분)

### 실습 코드:

```c
char s[] = "apple";
char *t = "banana";

printf("%s %s\n", s, t);
s[1] = 'A';   // 가능
t[1] = 'B';   // 런타임 오류 가능
```

* 문자열 리터럴은 수정 불가 → crash 경험
* 안전한 문자열 다루기 실습

---

## 실습 4: malloc/free 기초 (10분)

### 실습 코드:

```c
int n;
scanf("%d", &n);

int *arr = malloc(n * sizeof(int));

for (int i = 0; i < n; i++)
    arr[i] = i*i;

for (int i = 0; i < n; i++)
    printf("%d ", arr[i]);

free(arr);
```

* 메모리 누수 체크
* free 후 접근(댕글링) 테스트

---

## 실습 5: 구조체 + 포인터 실습 (10분)

### 코드:

```c
struct Student {
    char name[20];
    int score;
};

struct Student *p = malloc(sizeof(struct Student));

strcpy(p->name, "kim");
p->score = 95;

printf("%s %d\n", p->name, p->score);

free(p);
```

* `p->` 사용
* 구조체 메모리 동적 할당 실습

---

## 실습 6: 함수 포인터 미니 계산기 (10분)

### 코드:

```c
int add(int a, int b){ return a+b; }
int sub(int a, int b){ return a-b; }
int mul(int a, int b){ return a*b; }

int (*ops[3])(int, int) = { add, sub, mul };

int a, b, sel;
scanf("%d %d %d", &a, &b, &sel);

printf("결과 = %d\n", ops[sel](a, b));
```

* 함수 포인터 호출 실습
* “C에서도 다형성/콜백” 개념의 기초 경험

---

# 🎯 강의 마무리(5분)

* 오늘 배운 포인터 내용 전체 요약

* 포인터 핵심 키워드:

  * 주소
  * 타입
  * 역참조
  * 메모리 직접 접근
  * 동적 메모리
  * 함수 포인터(콜백)

* 학생들에게 나올 수 있는 과제 예시 제공:

  1. 동적 메모리 기반 평균 계산 프로그램
  2. 단일 연결 리스트 구현
  3. 함수 포인터 메뉴 기반 프로그램 만들기

---

# 원하시면…

* **PPT 20~25장 분량**으로 정리해 드릴 수 있고
* **교수님 강의용 스크립트**로 재구성도 가능합니다.
* 또는 **학생 실습용 문제지 + 해답 코드**도 제작해 드릴 수 있습니다.

어떤 형태로 확장하면 좋을까요?
